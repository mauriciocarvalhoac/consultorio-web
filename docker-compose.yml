services:
  consultorio-web:
    image: consultorio-web:1.0.0
    build:
      context: .
      dockerfile: Dockerfile
    container_name: consultorio-web
    ports:
      - "8000:80"
    restart: always